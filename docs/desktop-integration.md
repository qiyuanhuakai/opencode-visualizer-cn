# 桌面集成

Electron 中打开「设置 → 桌面」。Web 版不显示这些原生功能。

## 更新

- 应用与本机连接的 `vis_bridge` 分别检查更新；bridge 安装包在本机下载和执行。远程 bridge 仍展示连接版本，但需在其所在主机更新，不提供本机安装操作。
- Windows NSIS、Linux AppImage / DEB 安装版支持应用内下载安装；解压目录预览版不支持自动替换。
- macOS 保留 ad-hoc 签名：应用内检查与下载 DMG，确认后打开安装器，手动完成安装。
- `vis_bridge` 使用独立的 DEB / PKG / EXE 安装器。安装前会提示：安装器将停止守护进程，可能中断所有已连接客户端的任务。不会自动重启 bridge。
- 「安装器已打开」不是「安装成功」。Bridge 卡片显示当前已连接 bridge 的 `/healthz` 返回的版本，不回退为本机版本；旧 bridge 未提供版本字段时明确提示不可用，切换或断开连接后清除旧版本。仅回环地址（localhost、127/8、::1 及映射的 IPv4 回环地址）启用本机更新，其他地址保守视为远程；不能据此识别 SSH 隧道背后的真实主机。更新比较仍以连接版本为准（而非 `vis_bridge --version`），可用下载标为「本机安装包可用版本」。切换到远程或未知端点后，自动及手动更新均被原生服务阻止，旧下载和安装确认结果失效。
- 健康检查由应用根组件在应用生命周期内持有，设置窗口关闭时同样生效。每个连接代际都会分配新的连接 ID：先上报 `null`，健康检查成功后上报版本，失败或断开时上报 `null`；新的连接上报会使旧连接的进行中工作失效。手动「检查更新」会先刷新健康检查、等待上报被原生端接受，再触发原生检查，因此同一地址外部升级过的 bridge 不会沿用缓存版本；健康刷新失败或上报被拒绝时不会发起检查。
- OpenCode 后端同样使用「已配置的 Codex/共用 bridge 地址与令牌」做健康检查（默认 `ws://localhost:23004/codex`），而不是 OpenCode 服务地址（默认 `:4096`）；健康检查成功即证明该共用 bridge 可达，不代表 OpenCode 自身有任何 bridge 版本关联。无需 Codex WebSocket 已连接。
- 自动检查、自动下载默认关闭；启用后可在启动时检查并下载，安装始终需要确认。普通退出不会安装更新。
- 更新仅来自项目的正式 GitHub Release。手动安装包必须具有 SHA-256 摘要，下载后和打开前分别校验；错误会显示在更新卡片中。Node 下载请求支持 `HTTPS_PROXY` / `HTTP_PROXY` / `NO_PROXY` 环境代理。
- Windows 发布流程上传更新清单与独立 blockmap；Linux 上传更新清单，AppImage 的 blockmap 内嵌在安装包中，DEB 无独立 blockmap。只有正式发布、包含对应元数据的新版本才能提供应用自动更新；草稿不会被选中。

## 托盘与菜单

- 「最小化到托盘」和「关闭到托盘」独立设置，默认关闭。
- 托盘点击恢复窗口，托盘菜单可退出；真正退出仍执行本地编辑会话清理。
- 托盘使用正式应用图标；恢复最小化窗口后始终调用显示流程，避免 Windows 原生窗口已可见而渲染页面仍被标记为隐藏。
- 无可用托盘时不会隐藏唯一窗口。Linux 无桌面会话及未识别的托盘环境会保守禁用该能力。
- 原生菜单跟随应用语言，保留退出和「刷新」入口（Cmd/Ctrl+R），不显示「编辑」菜单；编辑命令仅用于保留剪切、复制、粘贴等快捷键。支持英语、简体中文、繁体中文、日语、世界语。

## 完成通知

- OpenCode 使用现有 SSE idle 通知；Codex 使用真实 `turn/completed`；ACP 使用实时 prompt 完成事件。
- Codex / ACP 的历史加载、取消、失败、断线及旧连接事件不会伪装成成功完成。
- 窗口可见且聚焦时也按通知与声音设置触发桌面完成提醒；关闭通知或声音仍分别遵循现有开关。点击通知可恢复窗口；切换后端后，旧通知不会选择错误后端的会话。浏览器前台通知抑制行为不变。
- 通知声音默认关闭。原生通知保持静音，由可选系统提示音提供单一声音源，避免双响。
- macOS 当前 ad-hoc 构建仍不支持原生通知，但可使用声音回退。系统免打扰、静音及通知权限可能阻止实际声音或通知展示。

## 本地会话存储

Electron 在应用用户目录中使用 `sessions.sqlite` 保存 Codex 的本地辅助历史（工具、思考片段），以及草稿、收藏和指定的 Codex / ACP / Kimi 本地元数据。后端服务自身的会话历史仍由对应后端管理。浏览器版继续使用 IndexedDB；较小的桌面设置保存在 `renderer-settings.json`。

首次启动自动导入旧 `renderer-storage.json` 和同名 `.history` 目录。历史和数据库元数据通过事务导入，完成后写入迁移标记；原文件保留且不再作为新数据的写入目标。导入失败不会提交部分历史或完成标记，并会阻止后续写入，修复源文件后重启可重试。不要用空数据库覆盖迁移失败的数据。

SQLite 由独立 worker 持有单一连接，使用 `DELETE` journal 和 `synchronous=FULL`。历史按消息和片段增量合并，通过异步 IPC 分页读取；其他窗口仅收到会话 ID 变更通知。显式清除会话保留迁移墓碑，避免旧浏览器缓存重新导入已删除记录。小型键值 API 保持同步接口，但写入确认在 worker 持久化完成后返回。

备份时先退出所有 Vis 窗口，再复制整个用户目录，包含数据库、设置和旧文件。旧 JSON 是迁移时的备份，不包含迁移后的新增记录；降级应用前应保留当前完整目录，不能把旧文件当作最新历史。

数据库回归测试已纳入 `pnpm test`，也可单独运行：

```sh
node --test scripts/qa/native-session-storage.test.mjs
```

## 本地验证

```sh
pnpm electron:preview
VIS_ELECTRON_EXECUTABLE=dist-electron/linux-unpacked/vis xvfb-run -a node scripts/qa/electron-desktop.mjs
```

可设置 `VIS_DESKTOP_QA_SERVER=http://127.0.0.1:4096`，驱动真实登录、设置交互与中英文截图；设置 `VIS_DESKTOP_QA_DOWNLOAD=1` 会实际下载并校验当前平台 bridge 安装包，但不会执行安装。用 `VIS_DESKTOP_QA_OUT` 指定证据目录。测试使用隔离用户目录并保持 Chromium 沙箱开启。
